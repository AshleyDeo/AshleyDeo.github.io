function preload(){
   /* let img = new Image();
    img.crossOrigin = "anonymous";
    img.src = loadImage("https://www.almanac.com/sites/default/files/image_nodes/moon-phases_delpixel-ss-resize2.jpg");*/
}

function setup(){
    let canvas = createCanvas(windowWidth - 20, windowHeight - 20);
}

function draw() {
    image(img, 0, 0);
}

function mouseMoved() {
    
}